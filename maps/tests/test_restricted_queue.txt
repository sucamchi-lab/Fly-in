# Test map: drones queued behind a restricted zone of capacity 1
# The landing slot is reserved on departure, so the next drone can only
# set off once the one ahead has left the gate for the goal.
nb_drones: 3

start_hub: start 0 0 [color=green]
hub: gate 1 0 [zone=restricted color=orange max_drones=1]
end_hub: goal 2 0 [color=red]

connection: start-gate
connection: gate-goal
